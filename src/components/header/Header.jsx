import React from "react";

import { StyledMainNav } from "../../styled/header";
import { StyledTitle } from "../../styled/header";

const Header = () => {
  return (
    <header>
      <StyledMainNav />
      <StyledTitle />
    </header>
  );
};

export default Header;
